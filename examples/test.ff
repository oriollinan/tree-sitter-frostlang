func : (int) -> int = v => {
    if v is 0 {
        return 0
    }
    from 1 to 10 by 1 |i: int| {
        print("{i}")
    }
    % for (int i = 0; i < 10; i++) {
    %     printf("%d\n", i);
    % }
    return 1
}

